openapi: 3.1.0
info:
  title: Api
  version: 0.1.0
  description: br(AI)n App API - Caregiver AI System
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: schedule
    description: Daily schedule and tasks (Q1-Q4 quarters)
  - name: symptoms
    description: Symptom and motivation logging
  - name: scripts
    description: Voice script management for Jessica
  - name: state
    description: Live app state management
  - name: haldol
    description: Haldol cycle tracking
  - name: governor
    description: Raymo's Sovereign Staff Governor - personal productivity pillars
  - name: gemini
    description: Gemini AI chat and image operations
  - name: smarthome
    description: Smart home device controls (Alexa/Sonos)
  - name: intercom
    description: E2EE encrypted family intercom
  - name: health-assessment
    description: Jessica health assessment sessions, questions, and data points
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /state:
    get:
      operationId: getAppState
      tags: [state]
      summary: Get current live app state (includes computed quarter from wall clock)
      responses:
        "200":
          description: Current app state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppState"
    put:
      operationId: updateAppState
      tags: [state]
      summary: Update live app state
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateAppStateInput"
      responses:
        "200":
          description: Updated app state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppState"

  /schedule:
    get:
      operationId: getSchedule
      tags: [schedule]
      summary: Get full daily schedule with Q1-Q4 tasks
      responses:
        "200":
          description: Daily schedule
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ScheduleTask"
    post:
      operationId: createScheduleTask
      tags: [schedule]
      summary: Create a new schedule task
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateScheduleTaskInput"
      responses:
        "201":
          description: Created task
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScheduleTask"

  /schedule/{id}:
    put:
      operationId: updateScheduleTask
      tags: [schedule]
      summary: Update a schedule task
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateScheduleTaskInput"
      responses:
        "200":
          description: Updated task
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScheduleTask"
    delete:
      operationId: deleteScheduleTask
      tags: [schedule]
      summary: Delete a schedule task
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  /schedule/{id}/complete:
    post:
      operationId: completeScheduleTask
      tags: [schedule]
      summary: Mark a task as completed
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Task marked complete
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScheduleTask"

  /symptoms:
    get:
      operationId: getSymptomLogs
      tags: [symptoms]
      summary: Get symptom logs (most recent first)
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: Symptom logs
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SymptomLog"
    post:
      operationId: createSymptomLog
      tags: [symptoms]
      summary: Log a symptom/behavior observation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSymptomLogInput"
      responses:
        "201":
          description: Created symptom log
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SymptomLog"

  /scripts:
    get:
      operationId: getVoiceScripts
      tags: [scripts]
      summary: Get all voice scripts for Jessica
      responses:
        "200":
          description: Voice scripts
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/VoiceScript"
    post:
      operationId: createVoiceScript
      tags: [scripts]
      summary: Create a new voice script
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateVoiceScriptInput"
      responses:
        "201":
          description: Created script
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VoiceScript"

  /scripts/{id}:
    put:
      operationId: updateVoiceScript
      tags: [scripts]
      summary: Update (patch) a voice script - live real-time update
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateVoiceScriptInput"
      responses:
        "200":
          description: Updated script
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VoiceScript"
    delete:
      operationId: deleteVoiceScript
      tags: [scripts]
      summary: Delete a voice script
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  /scripts/active:
    get:
      operationId: getActiveScripts
      tags: [scripts]
      summary: Get currently active scripts (for phone system)
      responses:
        "200":
          description: Active scripts for the phone system
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/VoiceScript"

  /haldol:
    get:
      operationId: getHaldolCycle
      tags: [haldol]
      summary: Get current Haldol 14-day cycle status
      responses:
        "200":
          description: Haldol cycle info
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HaldolCycle"
    put:
      operationId: updateHaldolCycle
      tags: [haldol]
      summary: Update Haldol cycle (set injection date, notes)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateHaldolCycleInput"
      responses:
        "200":
          description: Updated cycle
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HaldolCycle"

  /governor/pillars:
    get:
      operationId: getGovernorPillars
      tags: [governor]
      summary: Get all Governor productivity pillars
      responses:
        "200":
          description: Governor pillars
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/GovernorPillar"

  /governor/notes:
    get:
      operationId: getGovernorNotes
      tags: [governor]
      summary: Get synthesis notes (most recent first)
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: Governor notes
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/GovernorNote"
    post:
      operationId: createGovernorNote
      tags: [governor]
      summary: Log a synthesis note for a pillar
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateGovernorNoteInput"
      responses:
        "201":
          description: Created note
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GovernorNote"

  /gemini/conversations:
    get:
      operationId: listGeminiConversations
      tags: [gemini]
      summary: List all conversations
      responses:
        "200":
          description: List of conversations
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/GeminiConversation"
    post:
      operationId: createGeminiConversation
      tags: [gemini]
      summary: Create a new conversation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeminiConversationInput"
      responses:
        "201":
          description: Created conversation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeminiConversation"
  /gemini/conversations/{id}:
    get:
      operationId: getGeminiConversation
      tags: [gemini]
      summary: Get conversation with messages
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Conversation with messages
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeminiConversationWithMessages"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeminiError"
    delete:
      operationId: deleteGeminiConversation
      tags: [gemini]
      summary: Delete a conversation
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeminiError"
  /gemini/conversations/{id}/messages:
    get:
      operationId: listGeminiMessages
      tags: [gemini]
      summary: List messages in a conversation
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: List of messages
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/GeminiMessage"
    post:
      operationId: sendGeminiMessage
      tags: [gemini]
      summary: Send a message and receive an AI response (SSE stream)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeminiMessageInput"
      responses:
        "200":
          description: SSE stream of assistant response chunks
          content:
            text/event-stream: {}

  /smarthome/devices:
    get:
      operationId: getSmartHomeDevices
      tags: [smarthome]
      summary: Get all smart home devices and their state
      responses:
        "200":
          description: Device list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SmartHomeDevice"
  /smarthome/devices/{key}:
    put:
      operationId: updateSmartHomeDevice
      tags: [smarthome]
      summary: Update a device state
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateSmartHomeDeviceInput"
      responses:
        "200":
          description: Updated device
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SmartHomeDevice"

  /intercom/messages:
    get:
      operationId: getIntercomMessages
      tags: [intercom]
      summary: Get recent encrypted intercom messages
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
      responses:
        "200":
          description: Encrypted messages
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/IntercomMessage"
    post:
      operationId: postIntercomMessage
      tags: [intercom]
      summary: Post an encrypted message to the intercom
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateIntercomMessageInput"
      responses:
        "201":
          description: Posted message
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IntercomMessage"

  /health-assessment/questions:
    get:
      operationId: listHealthQuestions
      tags: [health-assessment]
      summary: List all health assessment questions
      responses:
        "200":
          description: List of questions
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/HealthQuestion"
    post:
      operationId: createHealthQuestion
      tags: [health-assessment]
      summary: Create a new health question
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateHealthQuestionInput"
      responses:
        "201":
          description: Created question
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthQuestion"

  /health-assessment/questions/{id}:
    put:
      operationId: updateHealthQuestion
      tags: [health-assessment]
      summary: Update a health question
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateHealthQuestionInput"
      responses:
        "200":
          description: Updated question
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthQuestion"
    delete:
      operationId: deleteHealthQuestion
      tags: [health-assessment]
      summary: Delete a health question
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  /health-assessment/sessions:
    get:
      operationId: listCallSessions
      tags: [health-assessment]
      summary: List call sessions (most recent first)
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 30
      responses:
        "200":
          description: List of sessions
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CallSession"
    post:
      operationId: startCallSession
      tags: [health-assessment]
      summary: Start a new call session
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StartCallSessionInput"
      responses:
        "201":
          description: Started session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CallSession"

  /health-assessment/sessions/{id}/end:
    put:
      operationId: endCallSession
      tags: [health-assessment]
      summary: End a call session
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Ended session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CallSession"

  /health-assessment/sessions/{id}/data-points:
    get:
      operationId: getSessionDataPoints
      tags: [health-assessment]
      summary: Get all health data points for a session
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: List of data points
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/HealthDataPoint"

  /health-assessment/summary/today:
    get:
      operationId: getTodaySummary
      tags: [health-assessment]
      summary: Get today's assessment summary grouped by category
      responses:
        "200":
          description: Today's summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssessmentSummary"

  /health-assessment/trends:
    get:
      operationId: getAssessmentTrends
      tags: [health-assessment]
      summary: Get 30-day trend data grouped by category and date
      responses:
        "200":
          description: Trend data
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TrendDataPoint"

  /health-assessment/anomalies:
    get:
      operationId: getAssessmentAnomalies
      tags: [health-assessment]
      summary: Get sustained anomaly categories (flagged in 3+ of last 5 sessions)
      responses:
        "200":
          description: Sustained anomalies
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssessmentAnomalies"

  /health-assessment/settings:
    get:
      operationId: getAssessmentSettings
      tags: [health-assessment]
      summary: Get assessment settings (quiet window, etc.)
      responses:
        "200":
          description: Settings object
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssessmentSettings"
    put:
      operationId: updateAssessmentSettings
      tags: [health-assessment]
      summary: Update assessment settings
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssessmentSettings"
      responses:
        "200":
          description: Updated settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssessmentSettings"

components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status

    AppState:
      type: object
      properties:
        id:
          type: integer
        currentQuarter:
          type: string
          enum: [Q1, Q2, Q3, Q4]
          description: "Effective quarter - override if set, else computed from wall clock"
        computedQuarter:
          type: string
          enum: [Q1, Q2, Q3, Q4]
          description: "Always the wall-clock-computed quarter regardless of override"
        quarterOverride:
          type: string
          nullable: true
          enum: [Q1, Q2, Q3, Q4]
          description: "Manual override set by Raymo. Null means use computed quarter."
        zombieMode:
          type: boolean
          description: "True when Pops is in rest mode (high symptom days 1-5 of Haldol cycle)"
        motivationLevel:
          type: integer
          minimum: 1
          maximum: 5
        lastUpdated:
          type: string
          format: date-time
        activeMessage:
          type: string
          description: "Message shown prominently on Pops' display"
        notes:
          type: string
        currentScheduledTask:
          nullable: true
          description: "Highest-priority active task for the current quarter (by order). Null if none."
          allOf:
            - $ref: "#/components/schemas/ScheduleTask"
      required:
        - id
        - currentQuarter
        - computedQuarter
        - zombieMode
        - motivationLevel
        - lastUpdated

    UpdateAppStateInput:
      type: object
      properties:
        quarterOverride:
          type: string
          nullable: true
          enum: [Q1, Q2, Q3, Q4]
        zombieMode:
          type: boolean
        motivationLevel:
          type: integer
          minimum: 1
          maximum: 5
        activeMessage:
          type: string
          nullable: true
        notes:
          type: string

    ScheduleTask:
      type: object
      properties:
        id:
          type: integer
        quarter:
          type: string
          enum: [Q1, Q2, Q3, Q4]
        timeLabel:
          type: string
          description: "e.g. '0600', '0800-1200'"
        title:
          type: string
        description:
          type: string
        voiceScript:
          type: string
          description: "What Jessica says when initiating this task"
        isCompleted:
          type: boolean
        completedAt:
          type: string
          format: date-time
          nullable: true
        order:
          type: integer
        isActive:
          type: boolean
      required:
        - id
        - quarter
        - timeLabel
        - title
        - isCompleted
        - order
        - isActive

    CreateScheduleTaskInput:
      type: object
      properties:
        quarter:
          type: string
          enum: [Q1, Q2, Q3, Q4]
        timeLabel:
          type: string
        title:
          type: string
        description:
          type: string
        voiceScript:
          type: string
        order:
          type: integer
      required:
        - quarter
        - timeLabel
        - title
        - order

    UpdateScheduleTaskInput:
      type: object
      properties:
        quarter:
          type: string
          enum: [Q1, Q2, Q3, Q4]
        timeLabel:
          type: string
        title:
          type: string
        description:
          type: string
        voiceScript:
          type: string
        order:
          type: integer
        isActive:
          type: boolean

    SymptomLog:
      type: object
      properties:
        id:
          type: integer
        loggedAt:
          type: string
          format: date-time
        ptsdTrigger:
          type: boolean
        hallucinationIntensity:
          type: integer
          minimum: 0
          maximum: 5
          description: "0 = none, 5 = severe"
        motivationLevel:
          type: integer
          minimum: 1
          maximum: 5
        behaviorNotes:
          type: string
        loggedBy:
          type: string
          description: "Who logged this (Raymo, Jessica, auto)"
      required:
        - id
        - loggedAt
        - hallucinationIntensity
        - motivationLevel

    CreateSymptomLogInput:
      type: object
      properties:
        ptsdTrigger:
          type: boolean
        hallucinationIntensity:
          type: integer
          minimum: 0
          maximum: 5
        motivationLevel:
          type: integer
          minimum: 1
          maximum: 5
        behaviorNotes:
          type: string
        loggedBy:
          type: string
      required:
        - hallucinationIntensity
        - motivationLevel

    VoiceScript:
      type: object
      properties:
        id:
          type: integer
        taskKey:
          type: string
          description: "Unique key tying this to a schedule task or trigger (e.g. 'morning_meds', 'wake_up')"
        label:
          type: string
          description: "Human-readable label for Raymo"
        scriptText:
          type: string
          description: "What Jessica says verbatim"
        tone:
          type: string
          enum: [gentle, grounding, urgent, encouraging, calm]
        isActive:
          type: boolean
        lastPatched:
          type: string
          format: date-time
          nullable: true
        patchNote:
          type: string
          description: "Why Raymo changed this script"
      required:
        - id
        - taskKey
        - label
        - scriptText
        - tone
        - isActive

    CreateVoiceScriptInput:
      type: object
      properties:
        taskKey:
          type: string
        label:
          type: string
        scriptText:
          type: string
        tone:
          type: string
          enum: [gentle, grounding, urgent, encouraging, calm]
        isActive:
          type: boolean
      required:
        - taskKey
        - label
        - scriptText
        - tone

    UpdateVoiceScriptInput:
      type: object
      properties:
        scriptText:
          type: string
        tone:
          type: string
          enum: [gentle, grounding, urgent, encouraging, calm]
        isActive:
          type: boolean
        patchNote:
          type: string

    HaldolCycle:
      type: object
      properties:
        id:
          type: integer
        lastInjectionDate:
          type: string
          format: date
          description: "Date of the last Haldol injection"
        cycleDay:
          type: integer
          description: "Current day within the 14-day cycle (1-14)"
        isZombiePhase:
          type: boolean
          description: "True on days 1-5 when symptoms are typically highest"
        nextInjectionDate:
          type: string
          format: date
        notes:
          type: string
      required:
        - id
        - lastInjectionDate
        - cycleDay
        - isZombiePhase
        - nextInjectionDate

    UpdateHaldolCycleInput:
      type: object
      properties:
        lastInjectionDate:
          type: string
          format: date
        notes:
          type: string

    GovernorPillar:
      type: object
      properties:
        id:
          type: integer
        pillarKey:
          type: string
          description: "productivity | passion | curiosity"
        name:
          type: string
        description:
          type: string
        focusDurationMins:
          type: integer
        metrics:
          type: array
          items:
            type: string
      required:
        - id
        - pillarKey
        - name
        - description
        - focusDurationMins
        - metrics

    GovernorNote:
      type: object
      properties:
        id:
          type: integer
        pillarKey:
          type: string
          nullable: true
        noteText:
          type: string
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - noteText
        - createdAt

    CreateGovernorNoteInput:
      type: object
      properties:
        pillarKey:
          type: string
        noteText:
          type: string
      required:
        - noteText

    GeminiConversation:
      type: object
      properties:
        id:
          type: integer
        title:
          type: string
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - title
        - createdAt
    GeminiMessage:
      type: object
      properties:
        id:
          type: integer
        conversationId:
          type: integer
        role:
          type: string
        content:
          type: string
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - conversationId
        - role
        - content
        - createdAt
    GeminiConversationInput:
      type: object
      properties:
        title:
          type: string
      required:
        - title
    GeminiMessageInput:
      type: object
      properties:
        content:
          type: string
      required:
        - content
    GeminiConversationWithMessages:
      type: object
      properties:
        id:
          type: integer
        title:
          type: string
        createdAt:
          type: string
          format: date-time
        messages:
          type: array
          items:
            $ref: "#/components/schemas/GeminiMessage"
      required:
        - id
        - title
        - createdAt
        - messages
    GeminiError:
      type: object
      properties:
        error:
          type: string
      required:
        - error

    SmartHomeDevice:
      type: object
      properties:
        id:
          type: integer
        deviceKey:
          type: string
        name:
          type: string
        type:
          type: string
        room:
          type: string
        isOn:
          type: boolean
        volume:
          type: integer
          nullable: true
        brightness:
          type: integer
          nullable: true
        meta:
          type: string
          nullable: true
        updatedAt:
          type: string
          format: date-time
      required:
        - id
        - deviceKey
        - name
        - type
        - room
        - isOn
        - updatedAt
    UpdateSmartHomeDeviceInput:
      type: object
      properties:
        isOn:
          type: boolean
        volume:
          type: integer
        brightness:
          type: integer
        meta:
          type: string

    IntercomMessage:
      type: object
      properties:
        id:
          type: integer
        sender:
          type: string
        ciphertext:
          type: string
        iv:
          type: string
        salt:
          type: string
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - sender
        - ciphertext
        - iv
        - salt
        - createdAt
    CreateIntercomMessageInput:
      type: object
      properties:
        sender:
          type: string
        ciphertext:
          type: string
        iv:
          type: string
        salt:
          type: string
      required:
        - sender
        - ciphertext
        - iv
        - salt

    HealthQuestion:
      type: object
      properties:
        id:
          type: integer
        text:
          type: string
        category:
          type: string
          enum: [mood, medication, sleep, appetite, cognition, voices, energy, task]
        responseType:
          type: string
          enum: [yes_no, scale_1_5, free_text]
        cycleDays:
          type: string
          nullable: true
          description: "JSON array of cycle day numbers when this question is prioritized. null = always ask"
        priority:
          type: integer
          minimum: 1
          maximum: 10
        alwaysAsk:
          type: boolean
        active:
          type: boolean
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - text
        - category
        - responseType
        - priority
        - alwaysAsk
        - active
        - createdAt

    CreateHealthQuestionInput:
      type: object
      properties:
        text:
          type: string
        category:
          type: string
        responseType:
          type: string
        cycleDays:
          type: string
          nullable: true
        priority:
          type: integer
        alwaysAsk:
          type: boolean
        active:
          type: boolean
      required:
        - text
        - category

    UpdateHealthQuestionInput:
      type: object
      properties:
        text:
          type: string
        category:
          type: string
        responseType:
          type: string
        cycleDays:
          type: string
          nullable: true
        priority:
          type: integer
        alwaysAsk:
          type: boolean
        active:
          type: boolean

    CallSession:
      type: object
      properties:
        id:
          type: integer
        conversationId:
          type: integer
          nullable: true
        sessionDate:
          type: string
          format: date
        cycleDay:
          type: integer
          nullable: true
        startedAt:
          type: string
          format: date-time
        endedAt:
          type: string
          format: date-time
          nullable: true
        summary:
          type: string
          nullable: true
        flagged:
          type: boolean
      required:
        - id
        - sessionDate
        - startedAt
        - flagged

    StartCallSessionInput:
      type: object
      properties:
        conversationId:
          type: integer
        cycleDay:
          type: integer
      required: []

    HealthDataPoint:
      type: object
      properties:
        id:
          type: integer
        sessionId:
          type: integer
        questionId:
          type: integer
          nullable: true
        category:
          type: string
        rawResponse:
          type: string
        parsedValue:
          type: string
          nullable: true
        parsedIntensity:
          type: string
          nullable: true
        flagged:
          type: boolean
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - sessionId
        - category
        - rawResponse
        - flagged
        - createdAt

    AssessmentSummary:
      type: object
      properties:
        sessionId:
          type: integer
          nullable: true
        sessionDate:
          type: string
          nullable: true
        cycleDay:
          type: integer
          nullable: true
        lastSessionStartedAt:
          type: string
          nullable: true
          description: "ISO timestamp of when today's session started"
        dataPoints:
          type: array
          items:
            $ref: "#/components/schemas/HealthDataPoint"
        categoryStatus:
          type: object
          description: "Map of category -> status (green/yellow/red)"
          additionalProperties:
            type: string
        flagged:
          type: boolean
        totalDataPoints:
          type: integer
      required:
        - flagged
        - totalDataPoints
        - dataPoints
        - categoryStatus

    TrendDataPoint:
      type: object
      properties:
        date:
          type: string
          format: date
        cycleDay:
          type: integer
          nullable: true
        category:
          type: string
        averageValue:
          type: number
          nullable: true
        count:
          type: integer
        flagged:
          type: boolean
      required:
        - date
        - category
        - count
        - flagged

    AssessmentAnomalies:
      type: object
      properties:
        sustainedAnomalies:
          type: array
          items:
            type: string
          description: Category names flagged in 3+ of the last 5 sessions
      required:
        - sustainedAnomalies

    AssessmentSettings:
      type: object
      properties:
        quietWindowStart:
          type: string
          description: "HH:MM format, e.g. 22:00"
        quietWindowEnd:
          type: string
          description: "HH:MM format, e.g. 07:00"
        engagementIntervalHours:
          type: integer
          description: "Hours of inactivity before Jessica initiates a check-in"
      required: []
